# Implementation Note
This folder contains data to test definition API integration without having to start an external server. Take not that search functionality will seem broken, as instead of a real search endpoint, just `definitions.json` is returned. 
